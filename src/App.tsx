import { Layout } from '@components/Layout';
import React from 'react';
interface AppProps {}

export const App = ({}: AppProps) => {
    return (
        <div>
            <Layout />
        </div>
    );
};
